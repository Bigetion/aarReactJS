// import { fromJS } from 'immutable';
// import { makeSelectPermissionPageDomain } from '../selectors';

// const selector = makeSelectPermissionPageDomain();

describe('makeSelectPermissionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
