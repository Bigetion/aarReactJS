// import { fromJS } from 'immutable';
// import { makeSelectRolePageDomain } from '../selectors';

// const selector = makeSelectRolePageDomain();

describe('makeSelectRolePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
